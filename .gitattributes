# Normalize all text files to LF in-repo (Linux-first workflow)
* text=auto eol=lf
# Keep Windows command scripts as CRLF when present
*.bat text eol=crlf
*.cmd text eol=crlf
*.tar filter=lfs diff=lfs merge=lfs
